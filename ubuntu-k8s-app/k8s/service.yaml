apiVersion: v1
kind: Service
metadata:
  name: ubuntu-app-service
  namespace: my-ubuntu-app
spec:
  selector:
    app: ubuntu-app
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081
