apiVersion: v1
kind: Service
metadata:
  name: html-app-service
  namespace: my-html-app
spec:
  selector:
    app: html-app
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30090
